==================================================
Part 1 – Index Page + Base Styling + Flexbox Basics
==================================================

Code to copy:
----------------------------------------
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <title>FlexPortal Dashboard – Index</title>
  <link rel="stylesheet" href="styles.css">
</head>
<body>
  <header class="header">
    <div class="logo">⚑ FlexPortal</div>
    <nav class="nav">
      <button class="nav-btn">Dashboard</button>
      <button class="nav-btn">Library</button>
      <button class="nav-btn">Player</button>
      <button class="nav-btn">Profile</button>
      <button class="nav-btn">About</button>
    </nav>
    <div class="actions">
      <button class="btn ghost">🌗 Theme</button>
    </div>
  </header>

  <main class="main">
    <aside class="sidebar">
      <h3>Sidebar</h3>
      <p>This section will later use Flex to stay fixed width.</p>
    </aside>

    <section class="content">
      <h2>Main Content Area</h2>
      <p>Everything here will expand with <code>flex: 1</code> later.</p>
    </section>
  </main>

  <footer class="footer">
    <p>Footer Section – status bar or controls</p>
  </footer>
</body>
</html>
----------------------------------------




==================================================
STEP 2 – Create styles.css 
==================================================

 Set theme colors, typography, spacing, and buttons:

Code to copy:
----------------------------------------
:root{
  --accent:#2196F3;
  --bg:#0f1116;
  --panel:#141720;
  --surface:#ffffff;
  --text:#0e0f12;
  --text-inv:#eef2ff;
  --gap:16px;
  --radius:12px;
}

*{box-sizing:border-box}
body{
  margin:0;
  font:16px/1.5 system-ui,Segoe UI,Roboto,Arial,sans-serif;
  background:var(--bg);
  color:var(--text);
}
a{color:inherit;text-decoration:none}
button{font:inherit;cursor:pointer}

.header,.footer,.sidebar{
  background:var(--panel);
  color:var(--text-inv);
  padding:12px var(--gap);
  border-radius:var(--radius);
  margin:var(--gap);
}
.content{
  background:var(--surface);
  color:var(--text);
  padding:16px;
  border-radius:var(--radius);
  margin:var(--gap);
}
.btn{background:#e9eef8;border:1px solid #d3d9e6;color:#0e0f12;padding:8px 12px;border-radius:10px}
.btn.ghost{background:transparent;border:1px solid #d3d9e6;color:var(--text-inv)}
.nav-btn{background:transparent;border:1px solid #293044;color:var(--text-inv);padding:8px 12px;border-radius:10px;margin:0 4px}
.nav-btn:hover{background:var(--accent);color:#fff;border-color:var(--accent)}
----------------------------------------
- open it on liveserver 


==================================================
STEP 3 – Add Flexbox Layouts (header → main → footer)
==================================================

──────────────────────────────
Part 3.1 – Flex Header
──────────────────────────────
What it does:
Turns the header into a horizontal bar: logo | nav | actions.


Code to copy (add to styles.css bottom):
----------------------------------------
.header{
  display:flex;
  justify-content:space-between;
  align-items:center;
  gap:var(--gap);
}

---------
.header .nav{
  display:flex;
  gap:8px;
  justify-content:center;
  flex:1;
}

-------

.header .actions{
  display:flex;
  gap:8px;
}

----------
- open it on liveserver  and see styles on devtools
Header items are now aligned in one clean horizontal line.



──────────────────────────────
Part 3.2 – Flex Main Layout
──────────────────────────────

Splits main area into two columns: sidebar (fixed width) + content (flexible).
Introduces nested flex and relative sizing.

Code to copy:
----------------------------------------
.main{
  display:flex;
  gap:var(--gap);
  margin:var(--gap);
}

--------

.sidebar{
  flex:0 0 240px;       /* fixed width */
}

-------

.content{
  flex:1;               /* take remaining space */
}

----------------------------------------
The sidebar sticks to the left, content fills the rest — our first big flex effect!



──────────────────────────────
Part 3.3 – Flex Footer
──────────────────────────────

Aligns footer text horizontally with space between left/right items.

----------------------------------------
.footer{
  display:flex;
  justify-content:space-between;
  align-items:center;
}
----------------------------------------
Footer text now spreads nicely; the whole page has a proper
Flex-based shell!

──────────────────────────────
 Part 3.4 – Responsive Adjustment
──────────────────────────────
Stack sidebar above content when the screen is narrow.

Code to copy:
----------------------------------------
@media(max-width:900px){
  .main{flex-direction:column}
  .header{flex-wrap:wrap}
}
----------------------------------------

──────────────────────────────
 Part 3.5 – Fill Content
──────────────────────────────

html, body {
  height: 100%;           /* make the body fill the full viewport height */
}

body {
  display: flex;           /* turn the whole body into a column flex container */
  flex-direction: column;  /* stack header, main, footer vertically */
}

.main {
  flex: 1;                 /* make main area expand to fill leftover space */
}

--------------------------------------------------------------------